import 'package:flutter/material.dart';

class CharityNotificationsComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Notifiactions"));
  }
}
